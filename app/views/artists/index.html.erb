<h1>All Artists</h1>
<p><%= link_to "New artist", new_artist_path %></p>
<ul>
<%= @artists.each do |artist| %>
  <li><%= link_to artist.name, artist_path(artist) %>
  <p><%= link_to "Edit", edit_artist_path(artist) %></p></li>
<% end %>
