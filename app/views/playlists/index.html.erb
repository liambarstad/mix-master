<h1>All Playlists</h1>
<%= link_to "New Playlist", new_playlist_path %>
<ul>
  <% @playlists.each do |playlist| %>
  <li>
    <a><%= link_to playlist.name, playlist_path(playlist) %></a>
    <a><%= link_to "Edit", edit_playlist_path(playlist) %></a>
  </li>

  <% end %>
</ul>
